================================================================================
🏗️ YOUTUBE MUSIC BOT - MODULAR ARCHITECTURE DIAGRAM
================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│                              📱 TELEGRAM USER                               │
│                                     ↓↑                                      │
└─────────────────────────────────────────────────────────────────────────────┘
                                      ↓↑
┌─────────────────────────────────────────────────────────────────────────────┐
│                            🚀 MAIN.PY (Entry Point)                         │
│  ┌────────────────────────────────────────────────────────────────────┐    │
│  │  • Validate Configuration                                          │    │
│  │  • Create Telegram Application                                     │    │
│  │  • Register Handlers                                               │    │
│  │  • Start Bot Polling                                               │    │
│  │  • Error Handling                                                  │    │
│  └────────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────────┘
                                      ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│                         📦 BOT PACKAGE MODULES                              │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │  ⚙️  CONFIG.PY (Configuration)                                        │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐ │ │
│  │  │  • TOKEN & ALLOWED_USERS                                        │ │ │
│  │  │  • Logging settings                                             │ │ │
│  │  │  • Player defaults                                              │ │ │
│  │  │  • MPV & YTDL options                                           │ │ │
│  │  │  • UI emojis & texts                                            │ │ │
│  │  └─────────────────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │  📨 HANDLERS/ (Telegram Handlers)                                    │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐ │ │
│  │  │  commands.py     → /start command                               │ │ │
│  │  │  callbacks.py    → Button clicks (play, pause, next, etc.)     │ │ │
│  │  │  messages.py     → URL messages (playlist/video)                │ │ │
│  │  └─────────────────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                            ↓                                                │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │  🎵 CORE/ (Core Functionality)                                        │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐ │ │
│  │  │  player_state.py  → State management (playlist, current song)  │ │ │
│  │  │  mpv_player.py    → MPV process control (start/stop/pause)     │ │ │
│  │  │  youtube.py       → YouTube data extraction (yt-dlp)           │ │ │
│  │  │  playback.py      → Playback logic (play/next/auto-next)       │ │ │
│  │  └─────────────────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                            ↓                                                │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │  🛠️  UTILS/ (Utilities)                                               │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐ │ │
│  │  │  keyboards.py       → Button layouts                           │ │ │
│  │  │  access_control.py  → User authentication                      │ │ │
│  │  │  formatters.py      → Message formatting                       │ │ │
│  │  └─────────────────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
                                      ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│                         💿 EXTERNAL SYSTEMS                                 │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐              │
│  │   MPV Player   │  │    YouTube     │  │   Telegram     │              │
│  │   (Streaming)  │  │   (yt-dlp)     │  │   Bot API      │              │
│  └────────────────┘  └────────────────┘  └────────────────┘              │
└─────────────────────────────────────────────────────────────────────────────┘

================================================================================
📊 DATA FLOW DIAGRAM
================================================================================

USER ACTION → HANDLER → ACCESS CHECK → CORE LOGIC → UPDATE STATE → RESPONSE

Example: User clicks "Play" button
┌──────────────────────────────────────────────────────────────────────────┐
│                                                                          │
│  User clicks [▶️ Play]                                                   │
│         ↓                                                                │
│  handlers/callbacks.py: button_callback()                               │
│         ↓                                                                │
│  utils/access_control.py: check_access() & is_owner()                   │
│         ↓                                                                │
│  handlers/callbacks.py: handle_play_pause()                             │
│         ↓                                                                │
│  core/playback.py: PlaybackManager.play_current_song()                  │
│         ↓                                                                │
│  core/mpv_player.py: MPVPlayer.start()                                  │
│         ↓                                                                │
│  core/player_state.py: Update player.is_playing = True                  │
│         ↓                                                                │
│  utils/formatters.py: Format "Now Playing" message                      │
│         ↓                                                                │
│  utils/keyboards.py: Generate updated keyboard                          │
│         ↓                                                                │
│  Send response to user                                                  │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘

================================================================================
🎯 MODULE DEPENDENCY GRAPH
================================================================================

                    ┌─────────────┐
                    │   main.py   │
                    └──────┬──────┘
                           │
         ┌─────────────────┼─────────────────┐
         │                 │                 │
    ┌────▼────┐      ┌─────▼──────┐   ┌─────▼─────┐
    │ config  │      │  handlers  │   │   core    │
    └─────────┘      └─────┬──────┘   └─────┬─────┘
                           │                 │
                     ┌─────┴─────┐     ┌─────┴─────┐
                     │           │     │           │
                ┌────▼────┐ ┌────▼────▼────┐ ┌────▼────┐
                │  utils  │ │     core     │ │ config  │
                └─────────┘ └──────────────┘ └─────────┘

================================================================================
🔄 PLAYBACK STATE MACHINE
================================================================================

    ┌──────────┐
    │  STOPPED │ ◄───────────────────────────────┐
    └────┬─────┘                                 │
         │ [Start]                               │ [Stop]
         ▼                                       │
    ┌──────────┐                                 │
    │  PLAYING │ ─────┐                          │
    └────┬─────┘      │ [Pause]                  │
         │            ▼                          │
         │       ┌────────┐                      │
         │       │ PAUSED │ ─────────────────────┤
         │       └────┬───┘      [Stop]          │
         │            │                          │
         │            │ [Resume]                 │
         │            ▼                          │
         │       ┌──────────┐                    │
         └──────►│  PLAYING │                    │
                 └────┬─────┘                    │
                      │                          │
                      │ [Song Ends]              │
                      ▼                          │
                 ┌──────────┐                    │
           ┌────►│AUTO-NEXT │                    │
           │     └────┬─────┘                    │
           │          │                          │
    [Loop] │          │ [No Loop]                │
           │          ▼                          │
           │     ┌──────────┐                    │
           └─────┤   NEXT   │                    │
                 │   SONG   │                    │
                 └────┬─────┘                    │
                      │ [Has Next]               │
                      ├─────────────►┐           │
                      │               ▼          │
                      │          ┌──────────┐    │
                      │          │  PLAYING │    │
                      │          └──────────┘    │
                      │                          │
                      │ [No Next & No Loop]      │
                      └──────────────────────────┘

================================================================================
📁 FILE ORGANIZATION
================================================================================

Project Root/
├── main.py                      ⭐ START HERE
│
└── bot/                         📦 Main package
    │
    ├── config.py                ⚙️  Configuration
    │   • TOKEN
    │   • ALLOWED_USERS
    │   • DEFAULT_VOLUME
    │   • EMOJI
    │   • MPV_OPTIONS
    │
    ├── core/                    🎵 Core Logic
    │   ├── player_state.py      → Song, PlayerState, player
    │   ├── mpv_player.py        → MPVPlayer (start/stop/pause)
    │   ├── youtube.py           → YouTubeExtractor
    │   └── playback.py          → PlaybackManager (play/next/prev)
    │
    ├── handlers/                📨 Telegram Handlers
    │   ├── commands.py          → start_command
    │   ├── callbacks.py         → button_callback + all handlers
    │   └── messages.py          → handle_url_message
    │
    └── utils/                   🛠️  Helpers
        ├── keyboards.py         → Keyboards (main_menu, volume_menu)
        ├── access_control.py    → AccessControl (check_access, is_owner)
        └── formatters.py        → MessageFormatter (all messages)

================================================================================
🎨 UI FLOW DIAGRAM
================================================================================

                     ┌─────────────────────┐
                     │   /start command    │
                     └──────────┬──────────┘
                                │
                                ▼
                     ┌─────────────────────┐
                     │   Welcome Message   │
                     │   + Main Menu       │
                     └──────────┬──────────┘
                                │
                ┌───────────────┼───────────────┐
                │               │               │
    ┌───────────▼────────┐      │      ┌────────▼──────────┐
    │  Load Playlist     │      │      │   Load Video      │
    └───────────┬────────┘      │      └────────┬──────────┘
                │               │               │
                ▼               │               ▼
    ┌────────────────────┐      │      ┌────────────────────┐
    │  Send Playlist URL │      │      │  Send Video URL    │
    └───────────┬────────┘      │      └────────┬───────────┘
                │               │               │
                └───────────────┼───────────────┘
                                │
                                ▼
                     ┌─────────────────────┐
                     │  Songs Added        │
                     │  Auto-Play Start    │
                     └──────────┬──────────┘
                                │
                                ▼
                     ┌─────────────────────┐
                     │   Control Panel     │
                     │  [▶️][⏭][⏮][⏹]      │
                     │  [🔁][🔀][🔊][📜]    │
                     └─────────────────────┘

================================================================================
🔐 ACCESS CONTROL FLOW
================================================================================

    User Action
         ↓
    ┌─────────────────┐
    │ Check Access    │ → ALLOWED_USERS empty? → Allow all
    │ (user_id)       │ → User in list? → Allow
    └────────┬────────┘ → Not in list? → Deny
             │
             ▼ [Allowed]
    ┌─────────────────┐
    │ Check Owner     │ → Owner not set? → Set & allow
    │ (user_id)       │ → User is owner? → Allow
    └────────┬────────┘ → Not owner? → Deny control actions
             │
             ▼ [Is Owner]
    ┌─────────────────┐
    │ Execute Action  │
    └─────────────────┘

================================================================================
💾 STATE MANAGEMENT
================================================================================

PlayerState (Singleton)
├── playlist: List[Song]           # All songs
├── current_index: int             # Current playing song index
├── is_playing: bool               # Playback status
├── is_paused: bool                # Pause status
├── loop_enabled: bool             # Loop mode
├── shuffle_enabled: bool          # Shuffle mode
├── volume: int                    # Volume (25/50/75/100)
├── mpv_process: Popen             # MPV subprocess
├── owner_id: int                  # Bot owner
└── playback_task: Task            # Async playback task

Song (Dataclass)
├── url: str                       # YouTube URL
├── title: str                     # Song title
└── duration: str                  # Duration in seconds

================================================================================
🚀 EXECUTION FLOW
================================================================================

1. STARTUP
   main.py
   ├── Import config
   ├── Validate config
   ├── Create Application
   ├── Register handlers
   └── Start polling

2. USER STARTS BOT
   /start command
   ├── handlers/commands.py
   ├── Check access
   ├── Set owner
   ├── Send welcome
   └── Show main menu

3. USER LOADS PLAYLIST
   User sends URL
   ├── handlers/messages.py
   ├── Validate URL
   ├── core/youtube.py → Extract
   ├── Add to playlist
   └── Auto-start playback

4. PLAYBACK
   PlaybackManager.play_current_song()
   ├── core/mpv_player.py → Start MPV
   ├── Update state
   ├── Send notification
   ├── Wait for finish
   └── Auto-next

5. USER CONTROLS
   Button click
   ├── handlers/callbacks.py
   ├── Route to handler
   ├── Call core function
   ├── Update state
   └── Send response

================================================================================

📖 For complete details, see ARCHITECTURE.md
🔄 For migration guide, see MIGRATION.md

================================================================================
